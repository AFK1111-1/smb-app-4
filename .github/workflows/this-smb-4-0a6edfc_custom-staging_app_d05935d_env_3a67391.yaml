name: >-
  this-smb-4-0a6edfc_custom-staging_app_d05935d_env_3a67391 Pipeline (Skyu
  Generated)
'on':
  push:
    branches:
      - staging
      - release/*
    paths-ignore:
      - .github/workflows/**
      - .trivyignore
      - Readme.md
      - README.md
  workflow_dispatch: {}
  repository_dispatch:
    types: pipeline_1df59a65-1f41-48b9-a9a8-cde6bc143636
env:
  ENCODED_PIPELINE_SECRET: ${{secrets.SMBAPP4_THISSMB40A6EDFC_CUSTOMSTAGING_APP_D05935D_ENV_3A67391}}
  COMMIT_ID: ${{github.event.client_payload.commitId || github.sha || github.run_id}}
  BRANCH_NAME: >-
    ${{github.event.client_payload.branchName || github.event.branch ||
    github.ref_name}}
  DEPLOY_STEP_SHOULD_DEPLOY: 'true'
jobs:
  code-scan:
    name: Code Scan
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: 'Run :: Checkout repository for Code Scan'
        uses: actions/checkout@v4
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Install Semgrep, Scan and Print'
        run: >-
          docker run -i -v "${PWD}:/src" semgrep/semgrep semgrep /src --json
          --quiet > semgrep_results.json
      - name: 'Run :: Trivy Filesystem Scan'
        run: |2-

                  docker run --rm -v "${PWD}:/src" aquasec/trivy fs /src --format json --quiet > trivy_results.json
                  # Minifying Json
                  jq -c '.' trivy_results.json > trivy_results.min.json
                  
      - name: 'Run :: Evaluate Trivy Results'
        run: |2-
             
                  if jq -e '.Results | map(select(.Vulnerabilities)) | length > 0' trivy_results.min.json >/dev/null; then
                    echo "Vulnerabilities detected!"
                    jq '.Results[] | select(.Vulnerabilities?) | {Target: .Target, Vulnerabilities: .Vulnerabilities[] | {ID: .VulnerabilityID, Severity: .Severity, Description: .Description}}' trivy_results.min.json
                    echo "IS_TRIVY_VULNERABILITIES_DETECTED=true" >> $GITHUB_ENV
                  fi
                  
      - name: 'Run :: Trivy Filesystem Scan (Including Trivy Ignores)'
        run: |2-

                  docker run --rm -v "${PWD}:/src" aquasec/trivy fs /src --format json --quiet --report summary --dependency-tree > trivy_results.json
                  # Minifying Json
                  jq -c '.' trivy_results.json > trivy_results.min.json
                  
      - name: 'Run :: Upload Code Scan Reports'
        env:
          RUN_ID: ${{ github.run_id }}
        run: |2-

                  echo "Uploading Code Scan Report" 
                  SARIF_FILE="semgrep_results.json"
                  if [ -n "$SARIF_FILE" ]; then
                    cat "$SARIF_FILE" > ${{ env.COMMIT_ID }}-code-scan-report.sarif
                    echo "Uploading Code Quality Results to S3 :: ${{ env.COMMIT_ID }}-code-scan-report.sarif"
           
                    response=$(curl -X POST -F 'file=@${{ env.COMMIT_ID }}-code-scan-report.sarif' -F 'provider=aws' -F 'resourceType=code-scan-report' -F 'cloudStoragePath=/${{ env.PIPELINE_APPLICATION_ID }}/code-scan-reports/' -F 'pipelineRunId=${{ env.RUN_ID }}'  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-auth-by: sa' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-resource-id: ${{ env.PIPELINE_APPLICATION_ID }}' -H 'Content-Type: multipart/form-data'  "${{ env.S3_UPLOAD_ENDPOINT }}")

                    if echo "$response" | grep -q '"success":true'; then
                      echo "SUCCESS :: $response"
                    else
                      echo "FAILED :: $response"
                    fi

                  else
                      echo "SARIF file not found."
                  fi

                  echo "Uploading Code Vulnerability Report" 
                  SARIF_FILE="trivy_results.min.json"
                  if [ -n "$SARIF_FILE" ]; then
                    cat "$SARIF_FILE" > ${{ env.COMMIT_ID }}-code-vulnerability-report.sarif
                    echo "Uploading Code Vulnerability Results to S3 :: ${{ env.COMMIT_ID }}-code-vulnerability-report.sarif"
           
                    response=$(curl -X POST -F 'file=@${{ env.COMMIT_ID }}-code-vulnerability-report.sarif' -F 'provider=aws' -F 'resourceType=code-vulnerability-report' -F 'cloudStoragePath=/${{ env.PIPELINE_APPLICATION_ID }}/code-vulnerability-reports/' -F 'pipelineRunId=${{ env.RUN_ID }}'  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-auth-by: sa' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-resource-id: ${{ env.PIPELINE_APPLICATION_ID }}' -H 'Content-Type: multipart/form-data'  "${{ env.S3_UPLOAD_ENDPOINT }}")

                    if echo "$response" | grep -q '"success":true'; then
                      echo "SUCCESS :: $response"
                    else
                      echo "FAILED :: $response"
                    fi

                  else
                      echo "SARIF file not found."
                  fi
                  
                  echo "Uploading Trivy Ignore File" 
                  if [ -f ".trivyignore" ]; then
                    response=$(curl -X POST -F 'file=@.trivyignore' -F 'provider=aws' -F 'resourceType=trivy-ignore' -F 'cloudStoragePath=/${{ env.PIPELINE_APPLICATION_ID }}/trivy-ignore-file/${{ env.COMMIT_ID }}/' -F 'pipelineRunId=$RUN_ID'  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-resource-id: ${{ env.PIPELINE_APPLICATION_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: multipart/form-data'  "${{ env.S3_UPLOAD_ENDPOINT }}")

                    if echo "$response" | grep -q '"success":true'; then
                      echo "SUCCESS :: $response"
                    else
                      echo "FAILED :: $response"
                    fi

                  else
                      echo ".trivyignore file not found"
                  fi
                  trivyVulnFound=${{ env.IS_TRIVY_VULNERABILITIES_DETECTED }}
                  if [ -n "$trivyVulnFound" ] && [ "NONE" != "NONE" ]; then
                    echo "Trivy vulnerabilities were detected."
                    echo "The job failed after uploading the code scan reports."
                    exit 1
                  fi
                  
  _status-pending:
    name: _status-pending
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Pipeline Started'
        env:
          RUN_ID: ${{ github.run_id }}
        run: |2-

              context=$(echo "$CONTEXT_JSON" | jq --arg sha "$COMMIT_ID" '.sha = $sha' | jq -c . | base64)
              response=$(curl -X PUT -d "runId=$RUN_ID&context=$context&appId=${{ github.event.client_payload.externalRepo.appId }}"  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.PIPELINE_SERVICE_EP }}/${{ env.PIPELINE_ID }}/PENDING?releaseId=${{ github.event.client_payload.releaseData.releaseId }}")
              if echo "$response" | grep -q '"success":true'; then
                echo "SUCCESS :: $response"
              else
                echo "FAILED :: $response"
              fi
              
    needs: []
    permissions: write-all
    env:
      CONTEXT_JSON: ${{ toJson(github) }}
    runs-on: ubuntu-latest
  _status_testing:
    name: _status_testing
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Pipeline Testing'
        run: |2-

              # NOTE: Do not remove this job even if you don't have tests at the moment.
              echo "Hello Tests from SkyU"
              
    needs:
      - _status-pending
    permissions: write-all
    runs-on: ubuntu-latest
  _release-info:
    name: _release-info
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Pipeline Release'
        env:
          RELEASE_ID: ${{ github.event.client_payload.releaseData.releaseId }}
          PIPELINE_ID: ${{ github.event.client_payload.releaseData.pipelineId }}
          RUN_ID: ${{ github.run_id }}
          APPLICATION_ID: ${{ github.event.client_payload.releaseData.appId }}
          USER_NAME: ${{ github.repository_owner }}
          REPOSITORY: ${{ github.repository }}
          COMMIT_USER: ${{ github.event.head_commit.author.name }}
          COMMIT_MESSAGE: ${{ github.event.client_payload.commitMessage }}
          COMMIT_TIME: ${{ github.event.head_commit.timestamp }}
          PROJECT_RELEASE_DATA: >-
            ${{
            toJson(github.event.client_payload.releaseData.projectReleaseData)
            }}
        run: |2-

              if [[ "$PROJECT_RELEASE_DATA" != "{}" && -n "$PROJECT_RELEASE_DATA" ]]; then
                echo "////////////////////////////"
                echo "Project Release Information"
                echo "////////////////////////////"
                echo "Project Release Tag  : $(echo "$PROJECT_RELEASE_DATA" | jq -r '.releaseTag')"
                echo "Previous Commit ID   : $(echo "$PROJECT_RELEASE_DATA" | jq -r '.commitData.preCommitId')"
                echo "New Commit ID        : $(echo "$PROJECT_RELEASE_DATA" | jq -r '.commitData.newCommitId')"
                echo "From                 : $(echo "$PROJECT_RELEASE_DATA" | jq -r '.envFrom')"
                echo "To                   : $(echo "$PROJECT_RELEASE_DATA" | jq -r '.envTo')"
              fi
              
              if [[ -n "${{ github.event.client_payload.releaseData.tag }}" ]]; then
                echo "////////////////////////////"
                echo "Application Release Information"
                echo "////////////////////////////"
                echo "Tag        : ${{ github.event.client_payload.releaseData.tag }}"
                echo "Title      : ${{ github.event.client_payload.releaseData.title }}"
              fi


              echo "////////////////////////////"
              echo "Pipeline Release Information"
              echo "////////////////////////////"
              echo "releaseId         : $RELEASE_ID"
              echo "pipelineId        : $PIPELINE_ID"
              echo "pipelineRunId     : $RUN_ID"
              echo "applicationId     : $APPLICATION_ID"
              echo "versionId         : ${{ env.DEPLOYMENT_APP_VERSION_ID }}"
              echo "credentialId      : ${{ env.PIPELINE_CREDENTIAL_ID }}"
              echo "userName          : $USER_NAME"
              echo "repositoryName    : $REPOSITORY"
              echo "commitId          : ${{ env.COMMIT_ID }}"
              echo "commitHash        : ${{ env.COMMIT_ID }}"
              echo "commitAvatar      : https://github.com/$COMMIT_USER.png"
              echo "commitTime        : $COMMIT_TIME"
              echo "commitMessage     : $COMMIT_MESSAGE"
              echo "commitUser        : $COMMIT_USER"
              echo "shouldDeploy      : ${{ env.DEPLOY_STEP_SHOULD_DEPLOY }}"
              echo "containerName     : ${{ env.DEPLOYMENT_CONTAINER_NAME }}"
              echo "imageRegistryURL  : ${{ env.IMAGE_NAME }}",
              # Storing Release Information for Deployment
              response=$(curl -X POST -d "releaseId=$RELEASE_ID&pipelineId=$PIPELINE_ID&pipelineRunId=$RUN_ID&versionId=${{ env.DEPLOYMENT_APP_VERSION_ID }}&applicationId=${{ github.event.client_payload.releaseData.appId }}&credentialId=${{ env.PIPELINE_CREDENTIAL_ID }}&userName=$USER_NAME&repositoryName=$REPOSITORY&commitId=${{ env.COMMIT_ID }}&commitData[commitHash]=${{ env.COMMIT_ID }}&commitData[commitAvatar]=https://github.com/$COMMIT_USER.png&commitData[commitTime]=$COMMIT_TIME&commitData[commitMessage]=$COMMIT_MESSAGE&commitData[commitUser]=$COMMIT_USER&shouldDeploy=${{ env.DEPLOY_STEP_SHOULD_DEPLOY }}&containerName=${{ env.DEPLOYMENT_CONTAINER_NAME }}&imageRegistryURL=${{ env.IMAGE_NAME }}&externalRepo[repositoryName]=${{ github.event.client_payload.externalRepo.externalRepoName }}&externalRepo[credentialId]=${{ github.event.client_payload.externalRepo.externalRepoCredId }}"  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.PIPELINE_SERVICE_EP }}/release/pipeline")
              if echo "$response" | grep -q '"success":true'; then
                echo "SUCCESS :: $response"
              else
                echo "FAILED :: $response"
              fi
              
    needs: []
    permissions: write-all
    if: ${{ github.event.client_payload.releaseData }}
    runs-on: ubuntu-latest
  build-and-release:
    name: build-and-release
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          cache: npm
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Install Ruby and Fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      - name: Install Fastlane
        run: |
          gem install fastlane
          bundle install
      - name: Setup Kinde environment variables
        run: |
          echo "Set Kinde Variables"
      - name: Create credentials directories
        run: |
          mkdir -p credentials/android
          mkdir -p credentials/ios
      # ANDROID STEPS COMMENTED OUT - iOS ONLY BUILD
      # - name: Setup Android Keystore from secrets
      #   run: >
      #     echo "${{ env.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode >
      #     credentials/android/release.keystore
      # - name: Setup Google Play Service Key
      #   run: |
      #     echo "$GOOGLE_PLAY_SERVICE_KEY" | base64 --decode > credentials/android/google-play-service-key.json
      # - name: Verify JSON (optional debug)
      #   run: |
      #     jq 'keys' credentials/android/google-play-service-key.json
      # Apple API Key NOT needed - using SSH key authentication with Match only
      # The Fastfile does not use app_store_connect_api_key anymore
      # - name: Setup Google Services JSON for Android
      #   run: >
      #     echo "${{ env.ANDROID_GOOGLE_SERVICES_JSON }}" | base64 --decode >
      #     google-services.json
      # - name: Setup Apple API Key for Fastlane Match
      #   run: |
      #     # Use the raw PEM key directly from the secret, GitHub preserves newlines
      #     echo "${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}" > credentials/ios/AuthKey.p8

      - name: Debug FASTLANE_PASSWORD Before Decode
        run: |
          echo "🔍 Checking FASTLANE_PASSWORD variable:"
          echo "Length: ${#FASTLANE_PASSWORD} characters"
          echo ""
          echo "First 100 chars of FASTLANE_PASSWORD:"
          echo "$FASTLANE_PASSWORD" | head -c 100
          echo ""
          echo ""
          echo "Checking if it's already valid base64:"
          if echo "$FASTLANE_PASSWORD" | base64 -d > /dev/null 2>&1; then
            echo "✅ Valid base64 string"
            echo ""
            echo "Decoded first 50 chars:"
            echo "$FASTLANE_PASSWORD" | base64 -d | head -c 50
            echo ""
          else
            echo "❌ NOT valid base64 - might already be decoded!"
            echo ""
            echo "Treating as raw content, first 50 chars:"
            echo "$FASTLANE_PASSWORD" | head -c 50
            echo ""
          fi
        env:
          FASTLANE_PASSWORD: ${{ env.FASTLANE_PASSWORD }}
          
      - name: Setup Apple API Key for Fastlane Match
        run: |
          mkdir -p credentials/ios
          
          # Check if FASTLANE_PASSWORD looks like raw PEM (starts with -----)
          if echo "$FASTLANE_PASSWORD" | head -c 5 | grep -q "^-----"; then
            echo "✅ FASTLANE_PASSWORD is raw PEM format, writing directly"
            echo "$FASTLANE_PASSWORD" > credentials/ios/AuthKey.p8.tmp
          else
            echo "📝 FASTLANE_PASSWORD is base64 encoded, decoding..."
            echo -n "$FASTLANE_PASSWORD" | base64 --decode > credentials/ios/AuthKey.p8.tmp
          fi
          
          # Fix: Remove spaces from base64 content and format properly
          echo "🔧 Formatting .p8 file properly..."
          
          # Extract just the base64 content (between headers)
          base64_content=$(sed -n '/BEGIN PRIVATE KEY/,/END PRIVATE KEY/p' credentials/ios/AuthKey.p8.tmp | \
            grep -v "BEGIN PRIVATE KEY" | \
            grep -v "END PRIVATE KEY" | \
            tr -d ' \n')
          
          # Write properly formatted .p8 file
          echo "-----BEGIN PRIVATE KEY-----" > credentials/ios/AuthKey.p8
          echo "$base64_content" | fold -w 64 >> credentials/ios/AuthKey.p8
          echo "-----END PRIVATE KEY-----" >> credentials/ios/AuthKey.p8
          
          rm credentials/ios/AuthKey.p8.tmp
          
          echo "✅ AuthKey.p8 created at: $(pwd)/credentials/ios/AuthKey.p8"
        env:
          FASTLANE_PASSWORD: ${{ env.FASTLANE_PASSWORD }}
          
      - name: Debug Apple Key File
        run: |
          echo "📁 Checking AuthKey.p8 file:"
          ls -lh credentials/ios/AuthKey.p8
          echo ""
          echo "File size: $(stat -f%z credentials/ios/AuthKey.p8) bytes"
          echo ""
          echo "🔍 Checking for null bytes:"
          if grep -q $'\0' credentials/ios/AuthKey.p8; then
            echo "❌ ERROR: File contains null bytes!"
            hexdump -C credentials/ios/AuthKey.p8 | head -20
          else
            echo "✅ No null bytes found"
          fi
          echo ""
          echo "🔍 File starts with (first 50 chars):"
          head -c 50 credentials/ios/AuthKey.p8
          echo ""
          echo ""
          echo "🔍 File ends with (last 50 chars):"
          tail -c 50 credentials/ios/AuthKey.p8
          echo ""
      - name: Setup Google Service Info Plist for iOS
        run: >
          echo "${{ env.IOS_GOOGLE_SERVICE_INFO_PLIST }}" | base64 --decode >
          GoogleService-Info.plist
      - name: Expo Prebuild (iOS only)
        run: npm run prebuild -- --clean --platform ios
      # - name: Build and Release Android
      #   run: fastlane android qa_release
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ env.FASTLANE_MATCH_DEPLOY_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts
      - name: Select Xcode 16.2
        run: |
          echo "Available Xcode versions:"
          ls -la /Applications/ | grep Xcode

          # ✅ Select the same version you use locally
          sudo xcode-select -s /Applications/Xcode_16.2.app

          echo "Selected Xcode path:"
          xcode-select -p
          echo "Xcode version details:"
          xcodebuild -version
      # - name: Select Xcode version
      #   uses: maxim-lobanov/setup-xcode@v1
      #   with:
      #     xcode-version: '16.4'
      # - name: Check available Xcode version
      #   run: |
      #     echo "Default Xcode version:"
      #     xcodebuild -version
      #     xcrun simctl list runtimes
      # - name: Select Xcode 16.0
      #   uses: maxim-lobanov/setup-xcode@v1
      #   with:
      #     xcode-version: '16.0'
      # - name: Select Xcode 16.1
      #   run: |
      #     echo "Available Xcode versions:"
      #     ls -la /Applications/ | grep Xcode

      #     # ✅ Select the same version you use locally
      #     # sudo xcode-select -s /Applications/Xcode_16.1.app
      #     # sudo xcode-select -s /Applications/Xcode.app
      #     sudo xcode-select -s /Applications/Xcode_16.0.app
          
      #     echo "Selected Xcode path:"
      #     xcode-select -p
      #     echo "Xcode version details:"
      #     xcodebuild -version
      # - name: Verify iOS project structure
      #   run: |
      #     echo "iOS project structure:"
      #     ls -la ios/
      #     echo "Workspace contents:"
      #     ls -la ios/smbmobile.xcworkspace/
      #     echo "Xcode project contents:"
      #     ls -la ios/smbmobile.xcodeproj/
      - name: Debug - Log Environment Variables
        run: |
          echo "=== Environment Variables Debug ==="
          echo ""
          echo "🔍 Checking if variables are set in shell:"
          [ -n "$APP_STORE_CONNECT_API_KEY_ID" ] && echo "✅ APP_STORE_CONNECT_API_KEY_ID: SET (length: ${#APP_STORE_CONNECT_API_KEY_ID})" || echo "❌ APP_STORE_CONNECT_API_KEY_ID: NOT SET"
          [ -n "$APP_STORE_CONNECT_ISSUER_ID" ] && echo "✅ APP_STORE_CONNECT_ISSUER_ID: SET (length: ${#APP_STORE_CONNECT_ISSUER_ID})" || echo "❌ APP_STORE_CONNECT_ISSUER_ID: NOT SET"
          [ -n "$MATCH_PASSWORD" ] && echo "✅ MATCH_PASSWORD: SET (length: ${#MATCH_PASSWORD})" || echo "❌ MATCH_PASSWORD: NOT SET"
          [ -n "$FASTLANE_PASSWORD" ] && echo "✅ FASTLANE_PASSWORD: SET (length: ${#FASTLANE_PASSWORD})" || echo "❌ FASTLANE_PASSWORD: NOT SET"
          [ -n "$TEST_KEY_123" ] && echo "✅ TEST_KEY_123: SET (length: ${#TEST_KEY_123})" || echo "❌ TEST_KEY_123: NOT SET"
          echo ""
          echo "🔓 Actual values (base64 encoded to bypass masking):"
          echo "APP_STORE_CONNECT_API_KEY_ID (base64): $(echo -n "$APP_STORE_CONNECT_API_KEY_ID" | base64)"
          echo "APP_STORE_CONNECT_ISSUER_ID (base64): $(echo -n "$APP_STORE_CONNECT_ISSUER_ID" | base64)"
          echo "MATCH_PASSWORD (base64): $(echo -n "$MATCH_PASSWORD" | base64 | head -c 30)... [truncated]"
          echo "FASTLANE_PASSWORD (base64): $(echo -n "$FASTLANE_PASSWORD" | base64 | head -c 30)... [truncated]"
          echo "TEST_KEY_123 (base64): $(echo -n "$TEST_KEY_123" | base64 | head -c 30)... [truncated]"
          echo ""
          echo "🔓 Decoded values (for verification):"
          echo "APP_STORE_CONNECT_API_KEY_ID: $(echo -n "$APP_STORE_CONNECT_API_KEY_ID" | base64 | base64 -d)"
          echo "APP_STORE_CONNECT_ISSUER_ID: $(echo -n "$APP_STORE_CONNECT_ISSUER_ID" | base64 | base64 -d)"
          echo ""
          echo "📋 All environment variables containing 'APP_STORE' or 'MATCH':"
          env | grep -E '(APP_STORE|MATCH)' | cut -d= -f1 | sort || echo "None found"
          echo ""
          echo "📋 All environment variables containing 'FASTLANE':"
          env | grep -E 'FASTLANE' | cut -d= -f1 | sort || echo "None found"
          echo ""
          echo "📋 All environment variables containing 'TEST':"
          env | grep -E 'TEST' | cut -d= -f1 | sort || echo "None found"
          echo ""
          echo "Node version: $(node --version)"
          echo "Ruby version: $(ruby --version)"
          echo "Fastlane version: $(fastlane --version)"
          echo "Current directory: $(pwd)"
          echo "==================================="
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ env.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ env.APP_STORE_CONNECT_ISSUER_ID }}
          MATCH_PASSWORD: ${{ env.MATCH_PASSWORD }}
          FASTLANE_PASSWORD: ${{ env.FASTLANE_PASSWORD }}
          TEST_KEY_123: ${{ env.TEST_KEY_123 }}
      - name: Build and Release iOS
        run: fastlane ios qa_release
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ env.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ env.APP_STORE_CONNECT_ISSUER_ID }}
          MATCH_PASSWORD: ${{ env.MATCH_PASSWORD }}
      # - name: Upload Android Release AAB
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: android-release-aab
      #     path: android/app/build/outputs/bundle/release/app-release.aab
      #     retention-days: '30'
      - name: Upload iOS Release IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-release-ipa
          path: ios/build/smbmobile.ipa
          retention-days: '30'
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            ## iOS QA Release v${{ github.run_number }}

            ### 📱 Build Artifacts
            - **🍎 iOS IPA**: Download from Actions tab

            ### 📋 Build Details
            - **Commit**: ${{ github.sha }}
            - **Branch**: ${{ github.ref_name }}
            - **Build Number**: ${{ github.run_number }}

            ### 🚀 Next Steps
            - Download and test the iOS build
            - Promote to staging when ready
          draft: false
          tag_name: qa-v${{ github.run_number }}
          prerelease: true
          name: iOS QA Release v${{ github.run_number }}
          token: ${{ env.GITHUB_TOKEN }}
    needs:
      - _status_testing
    permissions: write-all
    runs-on: macos-latest
  deploy:
    name: deploy
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Deploy and Notify Resource Service'
        env:
          RUN_ID: ${{ github.run_id }}
          COMMIT_USER: ${{ github.event.head_commit.author.name }}
          COMMIT_TIME: ${{ github.event.head_commit.timestamp }}
          COMMIT_MESSAGE: >-
            ${{ github.event.head_commit.message ||
            github.event.client_payload.commitMessage }}
        run: |2-

              if [ -z "$COMMIT_MESSAGE" ]; then
                COMMIT_MESSAGE=$(git log -1 --pretty=%B | tr '\n' ' ')
                echo "Fetched commit message: $COMMIT_MESSAGE"
                echo "COMMIT_MESSAGE=$COMMIT_MESSAGE" >> $GITHUB_ENV
              fi
            
              response=$(curl -X PATCH -d "imageRegistryURL=${{ env.IMAGE_NAME }}&appVersionId=${{ env.DEPLOYMENT_APP_VERSION_ID }}&tag=${{ env.COMMIT_ID }}&containerName=${{ env.DEPLOYMENT_CONTAINER_NAME }}&shouldDeploy=${{env.DEPLOY_STEP_SHOULD_DEPLOY}}&pipelineRunId=$RUN_ID&commitDetails[commitHash]=${{ env.COMMIT_ID }}&commitDetails[commitAvatar]=https://github.com/$COMMIT_USER.png&commitDetails[commitTime]=$COMMIT_TIME&commitDetails[commitMessage]=$COMMIT_MESSAGE&commitDetails[commitUser]=$COMMIT_USER"  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-application-id: ${{ env.PIPELINE_APPLICATION_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-resource-id: ${{ env.PIPELINE_APPLICATION_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.DEPLOYMENT_ENDPOINT }}")
              
              status_code=$?

              if [ $status_code -eq 0 ]; then
                if echo "$response" | grep -q '"success":true'; then
                  echo "SUCCESS :: $response"
                else
                  echo "FAILED :: $response"
                  exit 1
                fi
              else
                echo "Failed to deploy. HTTP status code: $status_code"
                exit 1
              fi
            
    needs:
      - build-and-release
      - _status_testing
    permissions: write-all
    if: github.event.client_payload.releaseData == null
    runs-on: ubuntu-latest
  _status-success:
    name: _status-success
    steps:
      - name: 'Run :: Start'
        run: |2-

              echo " Starting GitHub Action!" && 
              echo "STEPS_CAN_PROCEED=true" >> $GITHUB_ENV
              
      - name: 'Run :: Checkout repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 'Run :: Checkout Specific Commit'
        env:
          COMMIT_ID: ${{ github.event.client_payload.commitId }}
          BRANCH_NAME: ${{ github.event.client_payload.branchName }}
        if: >-
          ${{ github.event.client_payload.commitId != null ||
          github.event.client_payload.branchName != null }}
        run: |2-

              echo "Starting the checkout process..."
              git fetch --all
              if [ -n "$COMMIT_ID" ]; then
                echo "Commit ID provided: $COMMIT_ID"
                git checkout $COMMIT_ID || true
                echo "Checked out to commit ID: $COMMIT_ID"
              elif [ -n "$BRANCH_NAME" ]; then
                echo "Branch name provided: $BRANCH_NAME"
                git checkout $BRANCH_NAME || true
                echo "Checked out to branch: $BRANCH_NAME"

                #Updating the commit Id
                ACTUAL_COMMIT_ID=$(git rev-parse HEAD)
                echo "Changing CommitId: $ACTUAL_COMMIT_ID"
                echo "COMMIT_ID=$ACTUAL_COMMIT_ID" >> $GITHUB_ENV
              else
                echo "Skipping checkout..."
              fi
            
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Pipeline Success'
        env:
          RUN_ID: ${{ github.run_id }}
        run: |2-

              context=$(echo "$CONTEXT_JSON" | jq --arg sha "$COMMIT_ID" '.sha = $sha' | jq -c . | base64)
              response=$(curl -X PUT -d "runId=$RUN_ID&context=$context&appId=${{ github.event.client_payload.externalRepo.appId }}"  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.PIPELINE_SERVICE_EP }}/${{ env.PIPELINE_ID }}/SUCCESS?releaseId=${{ github.event.client_payload.releaseData.releaseId }}")
              
    needs:
      - code-scan
      - _status_testing
      - build-and-release
      - deploy
    permissions: write-all
    if: ${{ !(failure() || cancelled()) }}
    env:
      CONTEXT_JSON: ${{ toJson(github) }}
    runs-on: ubuntu-latest
  _status-failed:
    name: _status-failed
    steps:
      - name: 'Run :: Decoding Secrets'
        run: |2-

                if [ ! -x "$(command -v jq)" ]; then
                  echo "jq not found, installing..."
                  sudo apt-get update
                  sudo apt-get install -y jq
                fi
                echo "${{ env.ENCODED_PIPELINE_SECRET }}" | base64 --decode | jq -r 'to_entries[] | "\(.key)=\(.value)"' | while read line; do echo "$line" >> $GITHUB_ENV; echo "::add-mask::${line#*=}"; done
                
      - name: 'Run :: Pipeline Failed'
        env:
          RUN_ID: ${{ github.run_id }}
          GH_TOKEN: ${{ env.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          WF_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        run: |2-

              context=$(echo "$CONTEXT_JSON" | jq --arg sha "$COMMIT_ID" '.sha = $sha' | jq -c . | base64)
              response=$(curl -X PUT -d "runId=$RUN_ID&context=$context&appId=${{ github.event.client_payload.externalRepo.appId }}"  -H 'Authorization: ${{ env.SERVICE_ACC_ACCESS_TOKEN }}' -H 'x-organization-id: ${{ env.PIPELINE_ORGANIZATION_ID }}' -H 'x-project-id: ${{ env.PIPELINE_PROJECT_ID }}' -H 'x-environment-id: ${{ env.PIPELINE_ENVIRONMENT_ID }}' -H 'x-trace-id: ${{ env.PIPELINE_TRACE_ID }}' -H 'x-auth-by: sa' -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.PIPELINE_SERVICE_EP }}/${{ env.PIPELINE_ID }}/FAILED?releaseId=${{ github.event.client_payload.releaseData.releaseId }}")

              echo "Collecting workflow status..."
                
              JOB_DATA=$(curl -s       -H "Authorization: Bearer $GH_TOKEN"       "https://api.github.com/repos/$REPOSITORY/actions/runs/$RUN_ID/jobs")
                
              # FAILED job and step
              FAILED_JOB=$(echo "$JOB_DATA" | jq -r '
                .jobs[] | select(.conclusion=="failure") | .name' | head -n 1)
                
              FAILED_STEP=$(echo "$JOB_DATA" | jq -r '
                .jobs[].steps[] | select(.conclusion=="failure") | .name' | head -n 1)
                
              # CANCELLED job and step
              CANCELLED_JOB=$(echo "$JOB_DATA" | jq -r '
                .jobs[] | select(.conclusion=="cancelled") | .name' | head -n 1)
                
              CANCELLED_STEP=$(echo "$JOB_DATA" | jq -r '
                .jobs[].steps[] | select(.conclusion=="cancelled") | .name' | head -n 1)
                
              FAILED_MESSAGE=""
                
              if [ "$WF_CONCLUSION" = "cancelled" ]; then
                # Workflow cancelled
                if [ -n "$CANCELLED_JOB" ] && [ -n "$CANCELLED_STEP" ]; then
                    FAILED_MESSAGE="Workflow was cancelled while executing '$CANCELLED_STEP' step from '$CANCELLED_JOB' job"
                else
                    FAILED_MESSAGE="Workflow was cancelled"
                fi

              else
                # Workflow failed
                if [ -n "$FAILED_JOB" ] && [ -n "$FAILED_STEP" ]; then
                    FAILED_MESSAGE="Workflow failed while executing '$FAILED_STEP' step from '$FAILED_JOB' job"
                else
                    FAILED_MESSAGE="Workflow failed"
                fi
              fi

              # ALERT TITLE BUILD
              if [ -n "${{ env.APPLICATION_NAME }}" ]; then
                  ALERT_TITLE="${{ env.APPLICATION_NAME }} Pipeline Failed"
              else
                  ALERT_TITLE="Pipeline ${{ env.PIPELINE_ID }} Failed"
              fi

              # Triggering SkyU Alerts
              echo "ALERT TITLE :: $ALERT_TITLE"
              echo "ALERT MESSAGE :: $FAILED_MESSAGE"

              response=$(curl -X POST -d "labels[alertname]=pipelineFailed&labels[repository]=$REPOSITORY&labels[applicationName]=${{ env.APPLICATION_NAME }}&labels[applicationId]=${{ env.PIPELINE_APPLICATION_ID }}&labels[envLabel]=${{ env.PIPELINE_ENVIRONMENT_LABEL }}&labels[pipelineId]=${{ env.PIPELINE_ID }}&labels[system]=skyu&messageTitle=$ALERT_TITLE&message=$FAILED_MESSAGE&status=firing"  -H 'Content-Type: application/x-www-form-urlencoded'  "${{ env.FIRE_SKYU_ALERT_EP }}?organizationId=${{ env.PIPELINE_ORGANIZATION_ID }}&projectId=${{ env.PIPELINE_PROJECT_ID }}&environmentId=${{ env.PIPELINE_ENVIRONMENT_ID }}&token=${{ env.SERVICE_ACC_ACCESS_TOKEN_STRIPPED }}&authBy=sa")

              if echo "$response" | grep -q '"success":true'; then
                echo "SUCCESS :: $response"
              else
                echo "FAILED :: $response"
              fi
              
    needs:
      - code-scan
      - _status_testing
      - build-and-release
      - deploy
    permissions: write-all
    if: ${{ failure() || cancelled() }}
    env:
      CONTEXT_JSON: ${{ toJson(github) }}
    runs-on: ubuntu-latest